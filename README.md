# Zeotap Strings Mapper

## About

A dynamic single-page application that computes the characters found in two strings.

## Usage

bundle install
rake db:create
rake db:migrate
rails server
localhost:3000 
